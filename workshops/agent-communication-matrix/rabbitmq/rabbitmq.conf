# Load queue/exchange topology on first boot.
# User credentials are set via RABBITMQ_DEFAULT_USER / RABBITMQ_DEFAULT_PASS
# environment variables in docker-compose.yml.
management.load_definitions = /etc/rabbitmq/definitions.json
